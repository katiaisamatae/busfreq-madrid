# ============================================================
# BusFreq Madrid — dependencias de RUNTIME del dashboard
# Subconjunto ligero de requirements.txt: solo lo que necesita
# api.main para servir la API + la SPA (sin prophet, torch, shap,
# mlflow, jupyter, gtfs-kit, seaborn... que son para entrenar/EDA).
# Se usa en el Dockerfile.
# ============================================================

# ---- Datos ----
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # lectura de .parquet (pd.read_parquet) — imprescindible
python-dotenv>=1.0
requests>=2.31

# ---- Gráficos (optimizer usa matplotlib con backend Agg) ----
matplotlib>=3.8

# ---- Calendario de festivos (contexto + optimizer) ----
holidays>=0.50         # festivos ES/MD — usado por contexto.py y optimize.py

# ---- Optimización (Módulo 2) ----
ortools>=9.9

# ---- Backend API (Módulo 3) ----
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
